{
  "name": "ragkeep",
  "version": "0.2.1",
  "description": "--- language: - de tags: - books - rag - text-retrieval license: MIT pretty_name: ragkeep-weimarer-klassik-books-de configs: - default ---",
  "homepage": "https://github.com/cypherpunk-academy/ragkeep#readme",
  "bugs": {
    "url": "https://github.com/cypherpunk-academy/ragkeep/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cypherpunk-academy/ragkeep.git"
  },
  "license": "MIT",
  "author": "Michael Schmidt",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "push": "bash ./scripts/publish_to_hf.sh",
    "build:pages": "npm run build:registry && npm run apply:overrides && npm run build:app",
    "build:registry": "node --import tsx ./website/scripts/build_agent_registry.ts",
    "apply:overrides": "node --import tsx ./website/scripts/apply_overrides.ts",
    "build:app": "npm --prefix ./website/figma-prototype run build",
    "dev": "npm run build:registry && npm run apply:overrides && npm --prefix ./website/figma-prototype run dev"
  },
  "devDependencies": {
    "tsx": "latest",
    "typescript": "latest",
    "@types/node": "latest",
    "@types/js-yaml": "latest"
  },
  "dependencies": {
    "js-yaml": "latest"
  }
}